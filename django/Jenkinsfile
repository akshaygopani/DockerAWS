pipeline{
   agent any
   stages{
          stage('Build') {
              
            steps {      
                  sh '''
                     echo $PWD
                     sudo su jenkins
                     ./django/run-container.sh
                     '''
            }
        }   
   }
}
