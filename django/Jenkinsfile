pipeline{
   agent any
   stages{
          stage('push') {
              
            steps {      
                  sh '''
                     echo '-----generatig authentication token-----'
                     aws ecr get-login-password --region $REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$REGION.amazonaws.com

                     echo '-----building docker image------------'
                     sudo docker --context default build  ./django/sampleApp/nginx

                     echo '-----adding tag to image------------'
                     sudo docker --context default tag django_nginx:2.0 $AWS_ACCOUNT_ID.dkr.ecr.$REGION.amazonaws.com/django_nginx:2.0

                     echo '-----pushing docker image to ecr------------'
                     sudo docker --context default push $AWS_ACCOUNT_ID.dkr.ecr.$REGION.amazonaws.com/django_nginx:2.0

                     echo '-----building docker image------------'
                     sudo docker --context default build ./django

                     echo '-----adding tag to image------------'
                     sudo docker --context default tag sampleapp:2.0 $AWS_ACCOUNT_ID.dkr.ecr.$REGION.amazonaws.com/sampleapp:2.0

                     echo '-----pushing docker image to ecr------------'
                     sudo docker --context default push $AWS_ACCOUNT_ID.dkr.ecr.$REGION.amazonaws.com/django_nginx:2.0         
                     '''
            }
          }   
          stage('deploy') {              
            steps {      
                  sh '''
                     echo  '------creating a cloudformation stack for our app------'
                     sudo docker compose  -f docker-compose-aws.yml up
                     '''
            }   
        }   
   }
}
