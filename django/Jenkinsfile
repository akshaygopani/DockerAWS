pipeline{
   agent any
   stages{
          stage('Build') {
              
            steps {      
                  sh '''
                     echo $PWD
                     docker --context default container ls
                     '''
            }
        }   
   }
}
