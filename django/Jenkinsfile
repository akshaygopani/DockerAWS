pipeline{
   agent any
   stages{
          stage('Build') {
              
            steps {      
                  sh '''
                     echo $PWD
                     docker --contxt default container ls
                     '''
            }
        }   
   }
}
